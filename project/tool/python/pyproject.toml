[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "my_tool"
description = ""
requires-python = ">=3.7"
dependencies = []
version = "1.0.0"
